# Herbal Vision 功能图解

以下图解采用 Mermaid 时序图形式，风格上参考你给出的“功能图解/媒体同步”展示方式，适合直接截图放入论文，或继续在支持 Mermaid 的编辑器中微调。

```mermaid
sequenceDiagram
    autonumber
    actor U as 用户
    participant L as 登录与前端界面
    participant A as 认证与安全控制
    participant D as 检测服务
    participant M as 模型管理
    participant K as AI知识生成
    participant W as 目录监控
    participant C as RTSP/ONVIF监控中心
    participant H as 双模型对比与历史
    participant G as 日志中心
    participant O as 输出目录

    Note over U,O: Herbal Vision 中草药识别与流媒体智能监控系统功能图解

    U->>L: 访问系统首页
    L->>A: 登录认证 / 会话校验
    A-->>L: 登录成功，进入主控制台

    rect rgba(139, 207, 173, 0.15)
        Note over U,D: 图片/视频识别主流程
        U->>L: 上传单张图片 / 视频
        U->>L: 或上传多张草药图片
        L->>D: 调用识别接口
        D->>M: 加载当前模型并执行推理
        M-->>D: 返回检测框/类别/置信度
        D->>O: 保存标注结果到 outputs
        D->>K: 提交草药候选名称
        K-->>D: 返回草药简介/功效说明
        D-->>L: 返回检测结果 + AI说明
        L-->>U: 页面展示图像结果与知识卡片
    end

    rect rgba(103, 160, 255, 0.12)
        Note over U,H: 双模型对比分析流程
        U->>L: 上传同一批草药图片
        U->>L: 选择模型A / 模型B
        L->>H: 发起双模型对比
        H->>M: 分别加载模型A、模型B
        M-->>H: 返回两组检测结果
        H->>K: 提交对比统计信息
        K-->>H: 生成对比建议与优化反馈
        H->>O: 保存对比结果与历史记录
        H-->>L: 返回左右分栏结果 + 历史记录
        L-->>U: 展示模型差异，支持历史加载复盘
    end

    rect rgba(255, 180, 120, 0.12)
        Note over U,C: 实时监控识别流程
        U->>L: 配置 RTSP / ONVIF 摄像机
        L->>C: 保存监控源并启动监控
        C->>M: 持续获取视频帧并调用模型推理
        M-->>C: 返回每帧识别结果
        C-->>L: 输出 MJPEG 实时识别画面
        L-->>U: 浏览器实时预览检测结果
    end

    rect rgba(210, 210, 210, 0.12)
        Note over U,W: 后台自动化处理流程
        U->>L: 开启监控目录开关
        L->>W: 启动 watch 目录监听
        loop 定时/持续监听
            W->>D: 发现图片或视频后自动识别
            D->>O: 自动输出识别结果到指定目录
            D->>G: 记录处理日志
        end
        W-->>L: 返回处理状态与累计数量
    end

    rect rgba(200, 120, 220, 0.10)
        Note over U,G: 系统管理与辅助功能
        U->>L: 调整置信度 / IoU / 输入尺寸
        U->>L: 上传/切换用户自定义模型
        U->>L: 配置 AI API / 模型 / 密钥
        L->>M: 更新模型与推理参数
        L->>K: 更新AI配置
        L->>G: 请求系统日志与结构化结果
        G-->>L: 返回运行日志 / AI输出 / 状态信息
        L-->>U: 在参数设置页与日志中心统一展示
    end
```

## 论文配套说明文字

该功能图解反映了系统从“用户访问—身份认证—识别执行—AI补充—结果展示—后台监控—日志回溯”的完整工作链路。系统以前端控制台为统一交互入口，在用户登录成功后，可分别进入图片/视频识别、双模型对比、实时监控、参数设置和日志中心等模块。识别主流程中，系统调用当前模型完成目标检测，并将识别结果保存至输出目录，同时结合 AI 服务生成草药简介与功效说明。对于研究场景，系统还支持双模型对比和历史记录保存，便于研究者复盘分析；对于工程应用场景，系统则支持监控目录自动识别与 RTSP/ONVIF 实时监控接入，体现出一定的自动化处理能力与工程化集成能力。

## 使用建议

- 如果你要放进论文正文，可以直接截图 Mermaid 渲染结果。
- 如果你要我继续帮你做成“更像你发的那张图”的深色版 SVG/HTML 图，我下一步可以直接给你生成可导出图片的静态页面文件。
